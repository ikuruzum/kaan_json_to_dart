name: kaan_json_to_dart
version: 1.0.6
description: A library that generates Dart classes (parse and generator included) from a json string.
homepage: https://github.com/javiercbk/json_to_dart
environment:
  sdk: ">=2.12.0 <3.0.0"
dependencies:
  convert: "^3.0.1"
  dart_style: "^2.2.1"
  grapheme_splitter: ^1.0.1
  path: "^1.8.1"
dev_dependencies:
  test: "^1.20.1"

  lints: any

publish_to: none